# utop.js
Universal Top Level for JavaScript, or `utop.js` for short, is a shadcn registry that
distributes zero-dependency helper functions for working with common iterables in JavaScript.

```bash
pnpm dlx shadcn@latest add bathan1/utop/utop.js
```
