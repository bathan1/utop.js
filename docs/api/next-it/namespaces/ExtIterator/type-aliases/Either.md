---
title: Either
description: |-
  `Either<L, R>` is values of types `L` boxed in a `"left"` disciminateable
  object and `R` boxed in `"right"` kinds.
---

# Type Alias: Either\<L, R\>

> **Either**\<`L`, `R`\> = \{ `kind`: `"left"`; `value`: `L`; \} \| \{ `kind`: `"right"`; `value`: `R`; \}

Defined in: [ext-iterator/types.ts:9](https://github.com/bathan1/utop.js/blob/65acd6db7d1f070fc2dfb77c183a1c31994c8529/src/ext-iterator/types.ts#L9)

`Either<L, R>` is values of types `L` boxed in a `"left"` disciminateable
object and `R` boxed in `"right"` kinds.

## Type Parameters

### L

`L`

### R

`R`
